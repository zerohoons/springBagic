<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="members">

	<select id="list" resultType="com.ict.db.VO">
		select * from guestbook orderby idx
	</select>
	
	<insert id="insert" parameterType="com.ict.db.VO">
		insert into guestbook values()
	</insert>
	
	<select id="onelist" parameterType="String" resultType="com.ict.db.VO">
		select * form guestbook where idx=#{idx}
	</select>
	
	<delete id="delete" parameterType="String">
		delete form guestbook where idx=#{idx}
	</delete>
</mapper>
